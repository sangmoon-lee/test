version: 0.2

phases:
  install:
    runtime-versions:
        java: openjdk8     
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
artifacts:
  files:
    - target/test-0.0.1-SNAPSHOT.war
  discard-paths: yes
